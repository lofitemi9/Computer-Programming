Create a class named
    Square
give it two attributes
    length: int --> at least 2
    width: int --> at least 3
give it 1 constructor with 2 args
    pass the values to the setters
add a toString to summarize the Square
add method equals to determine if the length and width of the square are the same

Create another class with a main method
    Ask the user to input the length and width of two squares
    output the summary of each square.
    determine if both squares are equal